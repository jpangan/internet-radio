Country flag from an ISO 3166-1 alpha-2 code (3:2 raster via flagcdn.com), with a globe glyph fallback. Mirrors the app's `country-flag-icons` usage.

```jsx
<Flag code="GB" width={24} />
<Flag code={country.iso_3166_1} width={16} radius={2} />
```
