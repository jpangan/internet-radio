Country browse-list row: flag tile, country name, a station-count pill, and a chevron. Hover fills faintly.

```jsx
<CountryRow name="United Kingdom" code="GB" stationCount={2841} onClick={open} />
```
