Small uppercase status pill with a soft tinted fill and matching border. "Live" pulses a single dot; an accent badge containing "Connecting" bounces three dots.

```jsx
<Badge tone="live">Live</Badge>
<Badge tone="accent">Connecting…</Badge>
<Badge tone="neutral" showDot={false}>320 kbps</Badge>
```

- `tone`: `live` (green) | `accent` (violet) | `fav` (rose) | `neutral` (glass).
