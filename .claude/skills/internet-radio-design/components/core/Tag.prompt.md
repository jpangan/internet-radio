Tiny capitalized chip for genres / station metadata. Faint glass fill, no border by default; cluster several under a station name.

```jsx
<Tag>jazz</Tag> <Tag>news</Tag> <Tag accent>chillout</Tag>
```

- `accent`: violet tinted with a border (matches the share sheet).
